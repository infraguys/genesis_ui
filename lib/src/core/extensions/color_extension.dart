import 'dart:ui';

extension ColorExtension on Color {
  /// A simple placeholder that can be used to search all the hardcoded color
  /// in the code (useful to identify color that need to be named).
  Color get hardcoded => this;
}
